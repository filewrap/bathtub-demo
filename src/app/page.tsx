import { PagePlaceholder } from "@/components/shell/PagePlaceholder";

export default function HomePage() {
  return (
    <PagePlaceholder
      title="Home"
      lede="The hero, value promise, tub range, gallery preview, and customers atlas arrive in later work orders."
    />
  );
}
