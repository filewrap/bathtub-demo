/** Home Page value promise (AC-NYX-003.1), approved brand copy. */
export function ValuePromise() {
  return (
    <section aria-labelledby="promise-heading" className="reveal container-content py-24 md:py-40">
      <div className="mx-auto max-w-5xl text-center">
        <h2 id="promise-heading" className="text-display-s text-ink">
          We build rest you can drown the week in<span className="text-accent">.</span>
        </h2>
        <p className="mx-auto mt-10 max-w-xl text-lg text-ink-muted md:text-xl">
          Satisfaction guaranteed, or we never speak of it again.
        </p>
      </div>
    </section>
  );
}
