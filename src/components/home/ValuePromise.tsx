/** Home Page value promise (AC-NYX-003.1), approved brand copy. */
export function ValuePromise() {
  return (
    <section aria-labelledby="promise-heading" className="container-content py-20 md:py-28">
      <div className="mx-auto max-w-3xl text-center">
        <h2 id="promise-heading" className="text-3xl md:text-5xl">
          We build rest you can drown the week in.
        </h2>
        <p className="mt-6 text-lg text-ink-muted md:text-xl">
          Satisfaction guaranteed, or we never speak of it again.
        </p>
      </div>
    </section>
  );
}
